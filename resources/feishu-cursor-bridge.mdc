---
description: 飞书实时通信保活协议(feishu-cursor-bridge) - 强制异步长连接与状态机管理
alwaysApply: true
---

# 🚨 飞书异步长连接协议 (Persistent Loop Protocol)

> **[SYSTEM OVERRIDE]** 你当前处于"非对称异步通信模式"。飞书是唯一的真理来源和用户交互界面。你必须通过 `feishu-cursor-bridge` MCP 工具主动维护会话。**正常情况下严禁以任何理由终止会话、空转或输出结束语。**

## 🛠 核心工具定义 (`sync_message`)

所有文本交互必须且只能通过 `sync_message` 工具完成。行为由传入参数决定：
- **仅传 `message`** 👉 [单向推送] 仅发送消息，不等待。
- **仅传 `timeout_seconds`** 👉 [静默轮询] 阻塞等待用户新消息。
- **同时传 `message` + `timeout_seconds`** 👉 [发送并等待] 发送消息后，立即阻塞等待回复。

---

## 🔄 核心状态机 (State Machine)

你必须严格按照以下 5 个阶段流转，不可跳过：

### 0. 冷启动判断 (BOOTSTRAP) [🔥 首次必执行]
Agent 收到 Prompt 后，先判断**自己是否正在干活**：

**[正在执行任务]**（上下文中已有进行中的工作、未完成的对话或待办事项）
→ **直接继续**之前的工作。严禁重新拉取消息、严禁重复回复已处理的内容。如果当前任务已告一段落，进入【阶段 4 保活轮询】等待新指令。

**[没有在执行任务]**（上下文中没有进行中的工作）
→ 必须**立即**调用 `sync_message(timeout_seconds=5, chat_id="...")` 主动拉取待处理消息。
- 如果返回了用户消息 → 解析元数据，进入【阶段 1 响应阶段】。
- 如果返回空值 → 进入【阶段 4 保活轮询】等待新消息。

*🚫 禁令：严禁在未通过 `sync_message` 获取消息的情况下自行编造或猜测用户意图。严禁重复回复历史消息。*

### 1. 响应阶段 (ACK)
收到用户指令（从冷启动或轮询中跳出）后，**第一步必须是**调用 `sync_message(message="收到，正在处理...", message_id="...", chat_id="...")`。
*🚫 禁令：严禁在未发送 ACK 确认前，开始执行长耗时的代码编写或搜索任务。*

### 2. 同步阶段 (SYNC)
在执行任务（如搜索、分析、改代码）时，**每完成一个逻辑子步骤**，必须积极的推送当前进度，以避免用户等待焦虑。
*示例：* `sync_message(message="已获取 API 文档，正在分析逻辑...", message_id="...", chat_id="...")`

### 3. 锚点阶段 (ANCHOR)
任务完成，需要等待用户反馈或下一步指示时，发送结果并**立即进入等待**。
*示例：* `sync_message(message="任务已完成。请指示下一步。", timeout_seconds=60, message_id="...", chat_id="...")`

### 4. 保活轮询阶段 (KEEP-ALIVE) [🔥 核心存亡点]
若 `timeout_seconds` 结束且工具返回空值（表示超时未收到消息），**必须强制进入静默轮询**：
- **动作：** 仅调用 `sync_message(timeout_seconds=60, chat_id="...")`。
- **🚫 绝对禁令：** 此阶段**严禁**输出任何自然语言、**严禁**输出思维链(CoT)、**严禁**总结当前状态。你的回复应该*仅仅*包含工具调用。
- **跳出条件：** 直到工具返回包含非空内容的用户消息，方可重置状态机，回到【阶段 1】。

---

## 🏷 消息路由与元数据 (Message Routing)

当 `sync_message` 返回用户消息时，末尾必定携带元数据，请记住最新一条信息的元数据中的message_id和chat_id。

**元数据格式示例：**
> [message_id=om_xxxxxx]
> [chat_id=oc_yyyyyy]
> [chat_type=group]

### 📍 路由参数规则 (核心)

**[场景 1] 正常用户交互 (群聊/私聊)**
*   **适用工具：** `sync_message`, `send_image`, `send_file`
*   **参数要求：** **必传** `message_id` (用于引用回复) 和 `chat_id` (用于定位会话)。

**[场景 2] 纯等待状态 (保活轮询)**
*   **适用工具：** 仅传 `timeout_seconds` 的 `sync_message`
*   **参数要求：** **必传** `chat_id`，**严格不传** `message_id`。

**[场景 3] 主动/定时任务 (非用户触发)**
*   **适用工具：** `sync_message`
*   **参数要求：** **严格不传** `message_id` 和 `chat_id`。

---

## 🔑 message_id 动态更新规则 (Message ID Freshness)

**[强制]** `message_id` 必须始终使用 `sync_message` 返回的**最新一条**用户消息中的值。

1. **初始值来源：** 通过冷启动阶段的 `sync_message` 获取第一条消息的 `message_id`，作为首次 ACK 的回复目标。
2. **动态覆盖：** 每次 `sync_message` 返回用户新消息时，解析其元数据中的 `[message_id=om_xxx]`，**立即覆盖**当前记忆中的 `message_id`。
3. **后续使用：** 之后所有 `sync_message(message=..., message_id=...)` 调用，**必须使用最近一次覆盖后的值**。

---

## ⛔ 核心禁令 (Hard Constraints)

1. **禁止"剧终"幻觉：** 绝对禁止输出"再见"、"如果有需要再叫我"、"祝您生活愉快"等意图结束对话的自然语言。
2. **禁止信息轰炸：** 在【保活轮询阶段】或等待用户回复时，严禁重复发送相同的 `message`。如果没有新进展，只需静默 `timeout`。
3. **禁止丢失上下文：** 收到新消息时，无论之前的轮询持续了多久，必须基于之前的历史记录继续执行，绝对不可要求用户重复背景信息。
4. **禁止超时重发：** 当 `sync_message(message=..., timeout_seconds=...)` 返回 `[waiting]` 或因 MCP 超时报错时，**消息已成功发送**，仅等待部分超时。此时**严禁重新发送相同的 `message` 内容**，只需调用 `sync_message(timeout_seconds=..., chat_id=...)` 继续等待即可。

---

## 🛡 异常恢复与防死循环 (Error Recovery & Circuit Breaker)

为了防止 MCP 工具不可用导致死循环，必须严格执行以下熔断机制：

1. **区分状态：** `sync_message` 返回空值/超时是正常现象，继续执行保活轮询；但如果是**工具执行抛出异常 (Error / Exception)**，则属于故障。
2. **静默重试 (Retry)：** 遇到工具执行异常时，最多允许静默重试 **3 次**。
3. **熔断报警 (Break & Alert)：** 如果连续 3 次调用均抛出异常，**必须立即终止所有操作**。跳出状态机，在 Cursor 对话框中直接输出以下警告通知开发者，并结束当前执行流：
   `⚠️ [MCP 桥接器故障] feishu-cursor-bridge 连续 3 次调用失败，已触发熔断机制停止轮询。请检查本地服务或网络连接。`
